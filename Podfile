# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WowAppTest' do
    # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
    use_frameworks!
    
    # Pods for WowAppTest
    pod ‘SwiftyJSON’
    pod 'SDWebImage', '~>3.8'
    pod 'Alamofire', '~> 4.0'
    
    target 'WowAppTestTests' do
        inherit! :search_paths
        # Pods for testing
    end
    
    target 'WowAppTestUITests' do
        inherit! :search_paths
        # Pods for testing
    end
    
end
